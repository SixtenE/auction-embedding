/**
 * Must match the `vector(N)` dimension in Drizzle schema / migrations.
 * To change: update this value and generate a new SQL migration for the column.
 */
export const EMBEDDING_DIMENSIONS = 3072 as const
